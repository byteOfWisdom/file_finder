use cursive;

fn main() {
	let mut interface = build_interface();
	interface.
}


fn build_interface() -> cursive::Cursive{
	let mut interface = cursive::Cursive::default();

	interface.add_global_callback('q', |s| s.quit());

	return interface;
}